سلام! برنامه‌ریزی من برای انجام این تسک کامل شد. کارهایی که انجام خواهم داد به شرح زیر است:

۱. **حذف استخراج متنی (LLM) و جایگزینی با روش ویژن (Vision):**
در فایل `extractors/pdf_splitter.py`، منطق تصمیم‌گیری فعلی به گونه‌ای است که اگر مقدار متن صفحه از حد مشخصی بیشتر باشد، وضعیت صفحه `llm_required` قرار می‌گیرد. من این شرط را حذف خواهم کرد تا در صورتی که نتوانیم با Camelot جدولی پیدا کنیم، همیشه صفحه را به تصویر تبدیل کرده و به عنوان `image_required` ثبت کنیم تا فقط توسط Vision پردازش شود.

۲. **افزودن `transactional_power_score` به پرامپت‌ها:**
در فایل `analysis/account_router.py`، به خروجی مورد انتظار در دو پرامپت `SALARIED_ANALYSIS_PROMPT` و `PERSONAL_ANALYSIS_PROMPT` ویژگی `"transactional_power_score": "امتیاز از ۱ تا ۱۰"` را اضافه خواهم کرد تا مدل در زمان تحلیل این حساب‌ها نیز این امتیاز را تولید کند.

آیا با این تغییرات موافق هستید تا پیاده‌سازی را شروع کنم؟